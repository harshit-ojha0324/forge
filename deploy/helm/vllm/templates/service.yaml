apiVersion: v1
kind: Service
metadata:
  name: vllm
  labels:
    app.kubernetes.io/name: vllm
    app.kubernetes.io/part-of: forge
spec:
  selector:
    app.kubernetes.io/name: vllm
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
